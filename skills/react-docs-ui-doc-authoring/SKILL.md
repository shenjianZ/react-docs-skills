---
name: react-docs-ui-doc-authoring
description: Use this skill when the user asks how to write, organize, or maintain Markdown and MDX documentation in a docs project generated by create-react-docs-ui, including public/docs bilingual structure, frontmatter, page paths, sidebar-visible pages, custom MDX components, and document-writing conventions.
---

# React Docs UI 文档编写

当用户询问如何在生成后的 docs 项目里编写、组织、维护 Markdown 或 MDX 文档时，使用这个 skill。

默认假设用户正在一个生成后的 docs 项目中工作，例如 `D:\tmp\test-docs\docs`。

## 适用场景

- 用户想知道文档文件应该放在哪里
- 用户想写 Markdown 页面、frontmatter 或 MDX 页面
- 用户想知道自定义组件在 MDX 里怎么用
- 用户想知道中英文文档如何保持一致
- 用户想知道为什么新建了页面文件但导航或侧边栏没有显示

不要用这个 skill 回答具体配置字段语义；那类问题交给 `react-docs-ui-config`。

## 工作流程

1. 先判断用户要写的是普通 Markdown、带 frontmatter 的 Markdown，还是 MDX。
2. 根据目标 URL 推导应该落到 `public/docs/<lang>/` 下的哪个文件。
3. 如果页面还要出现在导航或侧边栏里，明确提醒还要同步修改配置文件。
4. 如果涉及 MDX 组件，说明组件通常放在 `src/components/`，必要时再结合配置处理。
5. 如果项目是双语站点，默认采用"先中文、后英文"的流程：先完成并审核 `zh-cn` 文档，再继续编写 `en` 文档。

## 输出约束

- 回答保持面向最终用户。
- 建议要紧贴 docs 项目的目录结构和使用方式。
- frontmatter 优先使用 `title`、`description`、`author`、`createdAt`、`lastUpdated` 这些字段。
- 如果 frontmatter 里已经写了 `title`，要提醒用户避免在正文里重复展示同一个标题。
- 默认优先产出中文 Markdown 或 MDX 文档；只有在中文内容审核没有问题后，再继续产出英文版本。英文文档应基于已确认的中文文档做等价翻译，不要先独立编写英文稿。
- 不要承诺"只建文件就会自动进入侧边栏"。

## 参考文件

- 需要说明目录和路由关系时，读取 `references/docs-structure.md`
- 需要说明 frontmatter 和 MDX 写法时，读取 `references/frontmatter-and-mdx.md`
- 需要说明双语维护规则时，读取 `references/bilingual-authoring.md`
